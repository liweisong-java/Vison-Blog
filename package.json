{
  "name": "vision-blog",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "pnpm --filter blog dev",
    "dev:blog": "pnpm --filter blog dev",
    "dev:publisher": "pnpm --filter publisher dev",
    "dev:video": "pnpm --filter video-to-blog dev",
    "publish:init": "pnpm --filter publisher dev init",
    "publish:doctor": "pnpm --filter publisher dev doctor",
    "publish:dry-run": "pnpm --filter publisher dev sync --dry-run",
    "publish:sync": "pnpm --filter publisher dev sync",
    "publish:auto-once": "pnpm --filter publisher dev auto-once",
    "publish:auto-install": "pnpm --filter publisher dev auto-install",
    "publish:auto-status": "pnpm --filter publisher dev auto-status",
    "publish:auto-uninstall": "pnpm --filter publisher dev auto-uninstall",
    "publish:server-run": "node scripts/server-publish-cycle.mjs",
    "video:init": "pnpm --filter video-to-blog dev init",
    "video:doctor": "pnpm --filter video-to-blog dev doctor",
    "video:enqueue": "pnpm --filter video-to-blog dev enqueue",
    "video:run": "pnpm --filter video-to-blog dev run",
    "video:status": "pnpm --filter video-to-blog dev status",
    "video:serve": "pnpm --filter video-to-blog dev serve",
    "video:install": "pnpm --filter video-to-blog dev server-install",
    "private:dashboard": "node scripts/generate-private-dashboard.mjs",
    "private:dashboard:traffic": "node scripts/fetch-umami-stats.mjs",
    "build": "pnpm --filter blog build && pnpm --filter publisher build && pnpm --filter video-to-blog build",
    "test": "pnpm --filter blog test && pnpm --filter publisher test && pnpm --filter video-to-blog test",
    "check": "pnpm --filter blog check && pnpm --filter publisher build && pnpm --filter video-to-blog build",
    "e2e": "pnpm --filter blog e2e"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "eslint": "^10.3.0",
    "playwright": "^1.59.1",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
