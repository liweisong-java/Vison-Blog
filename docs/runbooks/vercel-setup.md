# Vercel Setup

1. Import the repository into Vercel.
2. Set the root directory to `apps/blog`.
3. Set `SITE_URL` to the production domain.
4. Keep the output mode as static.
5. Optional: create a Deploy Hook and store it in `PUBLISH_DEPLOY_HOOK`.
