# 私有统计数据

这个目录只保留私有统计页的数据说明，不再提交真实统计快照。

- 真实的 `dashboard.json` 会生成到仓库根目录下的 `.superpowers/private-dashboard/dashboard.json`
- 博客页面构建时会优先读取上面的本地快照；如果缺失，则自动回退到空态结构
- 页面入口建议放在受 `Nginx Basic Auth` 保护的路径下
- 对应的 JSON 文件也必须一起保护，不能直接暴露到公网
- 公开仓库里不应提交任何真实访问统计、发布失败原因或内部运营数据
